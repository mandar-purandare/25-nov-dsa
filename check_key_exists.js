const obj = {name:'John', age:25, city:'New York'};
const key = 'age';

function doesKeyExist(obj,key){
    let objkey = key;
    // if(obj.objkey == undefined){return false} else{ return true};
    return obj.key;
}

console.log(doesKeyExist(obj,key));